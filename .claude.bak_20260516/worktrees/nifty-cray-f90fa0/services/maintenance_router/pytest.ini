[pytest]
# Run from services/maintenance_router/. The parent directory (services/) is
# added to sys.path so `import maintenance_router` resolves to this package.
pythonpath = ..
testpaths = tests
addopts = -ra
