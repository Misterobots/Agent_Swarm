# Turing Gateway & Ops Hub Docker Compose
# Routing handled by Saltbox's Traefik (connected to ai_lab_net externally)
# Hosts: Monitoring (prometheus, loki, hollerith/Grafana), ollama (Turing GPU)
# Compute services (Ollama, ComfyUI, etc.) remain on the Lovelace Execution Node

services:
  # Docker Sidecar Probe - exposes read-only container metadata for central monitoring
  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:latest
    container_name: docker-socket-proxy-turing
    restart: unless-stopped
    environment:
      - CONTAINERS=1
      - INFO=1
      - PING=1
      - VERSION=1
      - EVENTS=0
      - POST=0
      - AUTH=0
      - BUILD=0
      - IMAGES=0
      - NETWORKS=0
      - VOLUMES=0
      - SYSTEM=0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "2375:2375"
    networks:
      - ai_lab_net

  # ═══════════════════════════════════════════════════════════════════════════════
  # WORKLOAD IDENTITY (SPIRE Agent)
  # ═══════════════════════════════════════════════════════════════════════════════

  spire-agent:
    image: ghcr.io/spiffe/spire-agent:1.10.1
    container_name: spire-agent-turing
    hostname: spire-agent-turing
    restart: unless-stopped
    volumes:
      - ./config/spire:/etc/spire/agent
      - spire_agent_data:/run/spire/agent
      - spire_socket:/var/run/spire
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: ["-config", "/etc/spire/agent/agent.conf"]
    healthcheck:
      test: ["CMD", "/opt/spire/bin/spire-agent", "healthcheck", "-socketPath", "/var/run/spire/agent.sock"]
      interval: 30s
      timeout: 10s
      retries: 3
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_lab_net

  # ═══════════════════════════════════════════════════════════════════════════════
  # METRICS & OBSERVABILITY
  # ═══════════════════════════════════════════════════════════════════════════════

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus-turing
    hostname: prometheus-turing
    restart: unless-stopped
    volumes:
      - ./config/prometheus:/etc/prometheus
      - prometheus_data:/prometheus
    ports:
      - "9091:9090"
    mem_limit: 2g
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'   # 15 days is plenty for a home lab
      - '--storage.tsdb.retention.size=3GB'   # Hard cap — evicts oldest blocks first
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "${LOVELACE_HOST:-lovelace}:${LOVELACE_IP:-192.168.2.101}"
      - "${HOPPER_HOST:-control-node}:${HOPPER_IP:-192.168.2.102}"
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prometheus.rule=PathPrefix(`/prometheus`)"
      - "traefik.http.routers.prometheus.entrypoints=web"
      - "traefik.http.services.prometheus.loadbalancer.server.port=9090"

  alertmanager:
    image: prom/alertmanager:latest
    container_name: alertmanager-turing
    hostname: alertmanager-turing
    restart: unless-stopped
    depends_on:
      - maintenance-router    # webhook receiver must be reachable before alerts fire
    volumes:
      - ./config/alertmanager:/etc/alertmanager
      - alertmanager_data:/alertmanager
    ports:
      - "9093:9093"
    # Materialize $ALERT_SMTP_PASSWORD to /tmp/secrets/smtp_password (tmpfs)
    # before alertmanager starts, so alertmanager.yml can use auth_password_file
    # and the secret never appears on the filesystem outside the container.
    # /tmp is used because the alertmanager image runs as `nobody` and cannot
    # write to /run/secrets when mounted as tmpfs.
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        mkdir -p /tmp/secrets
        if [ -z "$$ALERT_SMTP_PASSWORD" ] || [ "$$ALERT_SMTP_PASSWORD" = "CHANGE_ME_APP_PASSWORD" ]; then
          echo "WARN: ALERT_SMTP_PASSWORD is not set; email alerts will fail to authenticate." >&2
        fi
        printf '%s' "$$ALERT_SMTP_PASSWORD" > /tmp/secrets/smtp_password
        chmod 0400 /tmp/secrets/smtp_password
        if [ -z "$$NTFY_ALERT_TOKEN" ]; then
          echo "WARN: NTFY_ALERT_TOKEN is not set; ntfy webhook will return 403." >&2
        fi
        printf '%s' "$$NTFY_ALERT_TOKEN" > /tmp/secrets/ntfy_token
        chmod 0400 /tmp/secrets/ntfy_token
        exec /bin/alertmanager --config.file=/etc/alertmanager/alertmanager.yml --storage.path=/alertmanager
    tmpfs:
      - /tmp/secrets:size=64k,mode=1777
    environment:
      - ALERT_SMTP_HOST=${ALERT_SMTP_HOST:-smtp.zoho.com}
      - ALERT_SMTP_PORT=${ALERT_SMTP_PORT:-587}
      - ALERT_SMTP_USERNAME=${ALERT_SMTP_USERNAME:-Admin@Graceful-Tech.com}
      - ALERT_SMTP_PASSWORD=${ALERT_SMTP_PASSWORD:-CHANGE_ME_APP_PASSWORD}
      - ALERT_EMAIL_FROM=${ALERT_EMAIL_FROM:-Admin@Graceful-Tech.com}
      - ALERT_EMAIL_TO=${ALERT_EMAIL_TO:-Justin@shivelymail.com}
      - NTFY_ALERT_TOPIC=${NTFY_ALERT_TOPIC:-home-ai-alerts}
      - NTFY_ALERT_TOKEN=${NTFY_ALERT_TOKEN:?set NTFY_ALERT_TOKEN - see turing_gateway/.env}
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.alertmanager.rule=PathPrefix(`/alertmanager`)"
      - "traefik.http.routers.alertmanager.entrypoints=web"
      - "traefik.http.services.alertmanager.loadbalancer.server.port=9093"

  # ═══════════════════════════════════════════════════════════════════════════════
  # MAINTENANCE ROUTER — Alertmanager → (auto_repair_daemon | Mission Control queue)
  # ═══════════════════════════════════════════════════════════════════════════════
  # Classifies incoming alerts using config/maintenance/manifest.yaml and
  # dispatches to either Redis (agent path → auto_repair_daemon) or the
  # Postgres-backed human queue surfaced in Mission Control.
  # Audit log: maintenance_dispatch table on Hopper Postgres.
  # ═══════════════════════════════════════════════════════════════════════════════

  maintenance-router:
    build:
      context: ../services/maintenance_router
      dockerfile: Dockerfile
    image: home-ai-lab/maintenance-router:latest
    container_name: maintenance-router
    hostname: maintenance-router
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ../config/maintenance:/etc/maintenance:ro
    environment:
      - MAINTENANCE_MANIFEST=/etc/maintenance/manifest.yaml
      - REDIS_HOST=redis-turing
      - REDIS_PORT=6379
      - MAINTENANCE_DB_HOST=${HOPPER_IP:-192.168.2.102}
      - MAINTENANCE_DB_PORT=5432
      - MAINTENANCE_DB_NAME=${MAINTENANCE_DB_NAME:-maintenance}
      - MAINTENANCE_DB_USER=${MAINTENANCE_DB_USER:-maintenance}
      - MAINTENANCE_DB_PASSWORD=${MAINTENANCE_DB_PASSWORD}
      - LOG_LEVEL=${MAINTENANCE_LOG_LEVEL:-INFO}
    ports:
      - "9095:9095"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:9095/healthz', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.maintenance-router.rule=PathPrefix(`/maintenance`)"
      - "traefik.http.routers.maintenance-router.entrypoints=web"
      - "traefik.http.services.maintenance-router.loadbalancer.server.port=9095"

  blackbox-exporter:
    image: prom/blackbox-exporter:latest
    container_name: blackbox-exporter-turing
    hostname: blackbox-exporter-turing
    restart: unless-stopped
    volumes:
      - ./config/blackbox:/etc/blackbox_exporter:ro
    ports:
      - "9115:9115"
    command:
      - '--config.file=/etc/blackbox_exporter/blackbox.yml'
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "${LOVELACE_HOST:-lovelace}:${LOVELACE_IP:-192.168.2.101}"
      - "${HOPPER_HOST:-control-node}:${HOPPER_IP:-192.168.2.102}"
    networks:
      - ai_lab_net

  loki:
    image: grafana/loki:latest
    container_name: loki-turing
    hostname: loki-turing
    restart: unless-stopped
    ports:
      - "3100:3100"
    volumes:
      - ./config/loki:/etc/loki
      - loki_data:/tmp/loki
    command: -config.file=/etc/loki/loki.yml
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.loki.rule=PathPrefix(`/loki`)"
      - "traefik.http.routers.loki.entrypoints=web"
      - "traefik.http.services.loki.loadbalancer.server.port=3100"

  promtail:
    image: grafana/promtail:latest
    container_name: promtail-turing
    hostname: promtail-turing
    restart: unless-stopped
    volumes:
      - ./config/promtail:/etc/promtail
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock
    command: -config.file=/etc/promtail/promtail.yml
    networks:
      - ai_lab_net

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: cadvisor-turing
    hostname: cadvisor-turing
    restart: unless-stopped
    command:
      - --housekeeping_interval=30s      # Reduce collection frequency (default 1s)
      - --docker_only=true               # Only monitor Docker containers, skip host metrics
      - --disable_metrics=udp,tcp,advtcp,sched,hugetlb,referenced_memory,resctrl,process
    ports:
      - "8888:8080"  # Use 8888 to avoid conflict with Traefik dashboard @ 8080
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    # /dev/kmsg removed: Ubuntu 22.04+ AppArmor blocks it even when passed through;
    # causes startup warning "operation not permitted" — OOM events disabled regardless
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.cadvisor.rule=PathPrefix(`/cadvisor`)"
      - "traefik.http.routers.cadvisor.entrypoints=web"
      - "traefik.http.services.cadvisor.loadbalancer.server.port=8080"

  hollerith:
    image: grafana/grafana:latest
    container_name: hollerith-turing
    hostname: hollerith-turing
    restart: unless-stopped
    ports:
      - "3001:3000"  # Port 3001 to avoid conflict with Langfuse @ 3000 and Open-WebUI
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD}
      - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER}
      - GF_SECURITY_SECRET_KEY=${GF_SECURITY_SECRET_KEY}
      - GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS=
      - GF_LOG_LEVEL=info
      - GF_SERVER_ROOT_URL=http://192.168.2.103/grafana/
      - GF_SERVER_SERVE_FROM_SUB_PATH=true
      - GF_SECURITY_ALLOW_EMBEDDING=true
      - GF_SECURITY_COOKIE_SAMESITE=lax
      - GF_SECURITY_COOKIE_SECURE=true
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
    volumes:
      - grafana_data:/var/lib/grafana
      - ./provisioning:/etc/grafana/provisioning:ro
      - ./dashboards:/etc/grafana/dashboards:ro
    depends_on:
      - prometheus
      - loki
      - alertmanager
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      # LAN route: 192.168.2.103/grafana
      - "traefik.http.routers.grafana.rule=PathPrefix(`/grafana`)"
      - "traefik.http.routers.grafana.entrypoints=web"
      - "traefik.http.routers.grafana.service=grafana"
      - "traefik.http.services.grafana.loadbalancer.server.port=3000"
      # External route: grafana.shivelymedia.com/grafana (via Cloudflare tunnel)
      # No Authentik - Grafana requires local Grafana authentication.
      - "traefik.http.routers.grafana-ext.rule=Host(`grafana.shivelymedia.com`)"
      - "traefik.http.routers.grafana-ext.entrypoints=web"
      - "traefik.http.routers.grafana-ext.priority=20"
      - "traefik.http.routers.grafana-ext.service=grafana-ext"
      - "traefik.http.services.grafana-ext.loadbalancer.server.port=3000"
      # Memex domain route: memex.shivelymedia.com/grafana/* → Grafana direct
      # Needed so the iframe in the Monitoring page bypasses Next.js proxy
      - "traefik.http.routers.grafana-memex.rule=Host(`memex.shivelymedia.com`) && PathPrefix(`/grafana`)"
      - "traefik.http.routers.grafana-memex.entrypoints=websecure"
      - "traefik.http.routers.grafana-memex.priority=30"
      - "traefik.http.routers.grafana-memex.tls=true"
      - "traefik.http.routers.grafana-memex.service=grafana"

  # ═══════════════════════════════════════════════════════════════════════════════
  # TASK QUEUE & PERSISTENCE
  # ═══════════════════════════════════════════════════════════════════════════════

  redis:
    image: redis:7.2-alpine
    container_name: redis-turing
    hostname: redis-turing
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes  # Enable AOF persistence
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - ai_lab_net
    # No Traefik labels — Redis accessed directly on port 6379, not via reverse proxy

  # ═══════════════════════════════════════════════════════════════════════════════
  # INFERENCE SERVICES (Dedicated safety + embeddings only after TTFT optimization)
  # All primary task models now consolidated on Lovelace (qwen3:14b).
  # Turing handles only: llama-guard-3:8b (async safety), nomic-embed (embeddings).
  # ═══════════════════════════════════════════════════════════════════════════════

  ollama:
    image: ollama/ollama:latest
    container_name: ollama-turing
    hostname: ollama-turing
    restart: always
    depends_on:
      spire-agent:
        condition: service_started
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      # RTX 3070 Ti settings (8GB VRAM) — safety + embeddings only
      - OLLAMA_HOST=0.0.0.0:11434
      - OLLAMA_GPU_OVERHEAD=512MiB
      - OLLAMA_FLASH_ATTENTION=1
      - OLLAMA_NUM_PARALLEL=1
      - OLLAMA_KEEP_ALIVE=-1            # Pin safety model permanently (fits in 8GB)
      - OLLAMA_METRICS=1               # Expose /metrics for Prometheus scraping
    volumes:
      - ollama_models:/root/.ollama
    ports:
      - "11434:11434"
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ollama.rule=PathPrefix(`/ollama`)"
      - "traefik.http.routers.ollama.entrypoints=web"
      - "traefik.http.services.ollama.loadbalancer.server.port=11434"
      - "spiffe.io/spiffe-id=spiffe://home-ai-lab/inference/ollama-turing"

  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui-turing
    hostname: open-webui
    restart: unless-stopped
    depends_on:
      - ollama
    environment:
      # Point to local Ollama
      - OLLAMA_BASE_URL=http://ollama:11434
      # Disable default OpenAI integration
      - OPENAI_API_BASE_URL=
      - OPENAI_API_KEY=
      # UI customization
      - WEBUI_DEFAULT_MODELS=nemotron-mini
      - WEBUI_NAME=MarsRL Hive Gateway
      - WEBUI_URL=http://192.168.2.103:3000
    volumes:
      - open-webui_data:/app/backend/data
    ports:
      - "3000:8080"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.open-webui.rule=PathPrefix(`/webui`)"
      - "traefik.http.routers.open-webui.entrypoints=web"
      - "traefik.http.services.open-webui.loadbalancer.server.port=8080"

  # ═══════════════════════════════════════════════════════════════════════════════
  # AGENT RUNTIME — FastAPI Backend (uvicorn :8000, exposed :8008)
  # Runs on Turing; memex_ui and Traefik reach it via ai_lab_net DNS (agent_runtime)
  # ═══════════════════════════════════════════════════════════════════════════════

  agent-runtime:
    image: home-ai-lab/agent-runtime:latest
    container_name: agent_runtime
    hostname: execution-node
    restart: unless-stopped
    env_file:
      - ../network.env
    environment:
      # OLLAMA_HOST = Lovelace (2× RTX 5060 Ti, 32 GB VRAM) — primary large-model host
      # SECONDARY_OLLAMA_HOST = Turing local ollama (RTX 3070 Ti, 8 GB) — guard + embeddings only
      - OLLAMA_HOST=http://${LOVELACE_IP:-192.168.2.101}:11434
      - SECONDARY_OLLAMA_HOST=http://ollama:11434
      - COMFYUI_HOST=http://${LOVELACE_IP:-192.168.2.101}:8188
      - PYTHONPATH=/app
      - MEMPALACE_API_URL=http://mempalace:8200
      - REDIS_HOST=${HOPPER_IP:-192.168.2.102}
      - REDIS_PORT=6379
      # GPU peer lock — Turing calls Lovelace's lock server when Redis is unavailable.
      # This is the critical env var that enables cross-host GPU coordination without Redis.
      # GPU_LOCK_SECRET is injected automatically via env_file (network.env).
      - GPU_LOCK_HOST=http://${LOVELACE_IP:-192.168.2.101}:8008
      # ── Model specialization (must match config.py defaults) ──────────────────
      # All large models live on Lovelace; OLLAMA_HOST above routes them there.
      # Turing local ollama only hosts llama-guard3:8b and nomic-embed-text.
      - PRIMARY_MODEL=${PRIMARY_MODEL:-qwen3.6:27b}
      - ROUTER_MODEL=${ROUTER_MODEL:-qwen3:8b}
      - COORDINATOR_MODEL=${COORDINATOR_MODEL:-gemma4:31b}
      - ARCHITECT_MODEL=${ARCHITECT_MODEL:-qwen3-coder:30b}
      - CODER_MODEL=${CODER_MODEL:-qwen3-coder:30b}
      - DEVOPS_MODEL=${DEVOPS_MODEL:-qwen3-coder:30b}
      - LIBRARIAN_MODEL=${LIBRARIAN_MODEL:-qwen3.6:27b}
      - RESEARCHER_MODEL=${RESEARCHER_MODEL:-qwen3.6:27b}
      - ANALYST_MODEL=${ANALYST_MODEL:-qwen3.6:27b}
      - VERIFIER_MODEL=${VERIFIER_MODEL:-qwen3:14b}
      - CONV_MODEL=${CONV_MODEL:-qwen3.6:27b}
      - SOLVER_MODEL=${SOLVER_MODEL:-qwen3-coder:30b}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - GITHUB_OAUTH_CLIENT_ID=${GITHUB_OAUTH_CLIENT_ID:-}
      - TOKEN_ENCRYPTION_KEY=${TOKEN_ENCRYPTION_KEY:-}
      - 'VALID_API_KEYS={"sk-coder-identity":"memex_ui","sk-devops-identity":"devops_agent"}'
    extra_hosts:
      - "mempalace:${HOPPER_IP:-192.168.2.102}"
    volumes:
      - ../agents:/app/agents
      - ../:/workspace
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: ["uvicorn", "agents.main:app", "--host", "0.0.0.0", "--port", "8000", "--log-level", "info"]
    ports:
      - "8008:8000"
    networks:
      - ai_lab_net

  # ═══════════════════════════════════════════════════════════════════════════════
  # UNIFIED UI (Hive Mind — Next.js)
  # ═══════════════════════════════════════════════════════════════════════════════

  memex-ui:
    build:
      context: ../ui
      dockerfile: Dockerfile
    image: home-ai-lab/memex-ui:latest
    container_name: memex_ui
    restart: unless-stopped
    env_file:
      - ../network.env
    environment:
      - API_BASE_URL=http://agent_runtime:8000
      - MEMPALACE_BASE_URL=http://mempalace:8200
      - LANGFUSE_URL=http://${HOPPER_IP:-192.168.2.102}:3000
      - LANGFUSE_PUBLIC_KEY=${LANGFUSE_PUBLIC_KEY}
      - LANGFUSE_SECRET_KEY=${LANGFUSE_SECRET_KEY}
      - HOPPER_IP=${HOPPER_IP:-192.168.2.102}
      - NEXT_PUBLIC_GATEWAY_URL=http://${TURING_IP:-192.168.2.103}
      - NEXT_PUBLIC_GRAFANA_URL=http://${TURING_IP:-192.168.2.103}/grafana
      - GRAFANA_INTERNAL_URL=http://hollerith-turing:3000/grafana
    extra_hosts:
      - "${LOVELACE_HOST:-lovelace}:${LOVELACE_IP:-192.168.2.101}"
      - "mempalace:${HOPPER_IP:-192.168.2.102}"
    volumes:
      - ../docs:/app/docs:ro
    ports:
      - "3200:3000"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000').then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - ai_lab_net
      - saltbox
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=saltbox"
      # Internal LAN route (with Authentik auth)
      - "traefik.http.routers.memex-ui.rule=PathPrefix(`/memex`)"
      - "traefik.http.routers.memex-ui.entrypoints=web"
      - "traefik.http.routers.memex-ui.middlewares=authentik@docker"
      - "traefik.http.routers.memex-ui.service=memex-ui"
      - "traefik.http.services.memex-ui.loadbalancer.server.port=3000"
      # External HTTPS route: memex.shivelymedia.com (behind Authentik)
      - "traefik.http.routers.memex-ext.rule=Host(`memex.shivelymedia.com`)"
      - "traefik.http.routers.memex-ext.entrypoints=websecure"
      - "traefik.http.routers.memex-ext.priority=20"
      - "traefik.http.routers.memex-ext.tls=true"
      - "traefik.http.routers.memex-ext.tls.certresolver=cfdns"
      - "traefik.http.routers.memex-ext.tls.domains[0].main=shivelymedia.com"
      - "traefik.http.routers.memex-ext.tls.domains[0].sans=*.shivelymedia.com"
      - "traefik.http.routers.memex-ext.tls.options=securetls@file"
      - "traefik.http.routers.memex-ext.service=memex-ext"
      - "traefik.http.routers.memex-ext.middlewares=globalHeaders@file,secureHeaders@file,robotHeaders@file,authentik@file"
      - "traefik.http.services.memex-ext.loadbalancer.server.port=3000"
      # External HTTP route: CF tunnel delivers traffic on web entrypoint — inject X-Forwarded-Proto and authenticate
      - "traefik.http.routers.memex-ext-http.rule=Host(`memex.shivelymedia.com`)"
      - "traefik.http.routers.memex-ext-http.entrypoints=web"
      - "traefik.http.routers.memex-ext-http.priority=20"
      - "traefik.http.routers.memex-ext-http.service=memex-ext-http"
      - "traefik.http.routers.memex-ext-http.middlewares=globalHeaders@file,cf-https-proto@file,robotHeaders@file,authentik@file"
      - "traefik.http.services.memex-ext-http.loadbalancer.server.port=3000"
      # Static asset bypass — no auth for Next.js assets to prevent post-login redirect to /manifest.json
      # Browser fetches these in the background; without bypass they hijack the Authentik redirect state
      # HTTPS bypass (websecure): static assets served directly, no ForwardAuth
      - "traefik.http.routers.memex-static.rule=Host(`memex.shivelymedia.com`) && (PathPrefix(`/_next/`) || Path(`/manifest.json`) || Path(`/favicon.ico`) || Path(`/robots.txt`) || Path(`/sitemap.xml`))"
      - "traefik.http.routers.memex-static.entrypoints=websecure"
      - "traefik.http.routers.memex-static.priority=30"
      - "traefik.http.routers.memex-static.tls=true"
      - "traefik.http.routers.memex-static.service=memex-ext"
      # HTTP bypass (web): same paths on port 80 — redirect to HTTPS only, no authentik
      # Static HTTP bypass: inject X-Forwarded-Proto for CF tunnel, no auth gate
      - "traefik.http.routers.memex-static-http.rule=Host(`memex.shivelymedia.com`) && (PathPrefix(`/_next/`) || Path(`/manifest.json`) || Path(`/favicon.ico`) || Path(`/robots.txt`) || Path(`/sitemap.xml`))"
      - "traefik.http.routers.memex-static-http.entrypoints=web"
      - "traefik.http.routers.memex-static-http.priority=30"
      - "traefik.http.routers.memex-static-http.middlewares=cf-https-proto@file"
      - "traefik.http.routers.memex-static-http.service=memex-ext-http"
      # WebSocket terminal — bypasses Next.js, routes directly to execution-node backend
      # LAN: ws://[gateway]/ws/terminal  HTTPS: wss://memex.shivelymedia.com/ws/terminal
      - "traefik.http.routers.terminal-ws.rule=PathPrefix(`/ws/terminal`)"
      - "traefik.http.routers.terminal-ws.entrypoints=web"
      - "traefik.http.routers.terminal-ws.priority=30"
      - "traefik.http.routers.terminal-ws.service=terminal-ws"
      - "traefik.http.routers.terminal-ws.middlewares=authentik@docker"
      - "traefik.http.services.terminal-ws.loadbalancer.server.url=http://agent_runtime:8000"
      - "traefik.http.routers.terminal-ws-ext.rule=Host(`memex.shivelymedia.com`) && PathPrefix(`/ws/terminal`)"
      - "traefik.http.routers.terminal-ws-ext.entrypoints=websecure"
      - "traefik.http.routers.terminal-ws-ext.priority=30"
      - "traefik.http.routers.terminal-ws-ext.tls=true"
      - "traefik.http.routers.terminal-ws-ext.service=terminal-ws"
      - "traefik.http.routers.terminal-ws-ext.middlewares=globalHeaders@file,authentik@docker"

  # ═══════════════════════════════════════════════════════════════════════════════
  # OPENCLAUDE gRPC INFERENCE GATEWAY (Phase 6)
  # ═══════════════════════════════════════════════════════════════════════════════

  openclaude-grpc:
    build:
      context: ..
      dockerfile: agents/grpc/Dockerfile
    image: home-ai-lab/openclaude-grpc:latest
    pull_policy: build
    container_name: openclaude_grpc
    hostname: openclaude-grpc
    restart: unless-stopped
    env_file:
      - ../network.env
    environment:
      - OLLAMA_HOST=http://${LOVELACE_IP:-192.168.2.101}:11434
      - SECONDARY_OLLAMA_HOST=http://ollama:11434
      - ARCHITECT_MODEL=${ARCHITECT_MODEL:-qwen3-coder:30b}
      - COORDINATOR_MODEL=${COORDINATOR_MODEL:-gemma4:31b}
      - LIBRARIAN_MODEL=${LIBRARIAN_MODEL:-qwen3.6:27b}
      - GRPC_PORT=50051
      - GRPC_MAX_WORKERS=4
      - GRPC_AUTH_ENABLED=${GRPC_AUTH_ENABLED:-true}
      - AUTHENTIK_URL=${AUTHENTIK_URL:-http://authentik:9000}
    ports:
      - "50051:50051"
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "${LOVELACE_HOST:-lovelace}:${LOVELACE_IP:-192.168.2.101}"
    healthcheck:
      test: ["CMD", "python", "-c", "import grpc; ch=grpc.insecure_channel('localhost:50051'); grpc.channel_ready_future(ch).result(timeout=3)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      # gRPC uses HTTP/2, Traefik routes via h2c for internal LAN
      - "traefik.http.routers.openclaude-grpc.rule=PathPrefix(`/grpc`)"
      - "traefik.http.routers.openclaude-grpc.entrypoints=web"
      - "traefik.http.routers.openclaude-grpc.service=openclaude-grpc"
      - "traefik.http.services.openclaude-grpc.loadbalancer.server.port=50051"
      - "traefik.http.services.openclaude-grpc.loadbalancer.server.scheme=h2c"
      # External HTTPS route: grpc.shivelymedia.com (behind Authentik)
      - "traefik.http.routers.openclaude-ext.rule=Host(`grpc.shivelymedia.com`)"
      - "traefik.http.routers.openclaude-ext.entrypoints=websecure"
      - "traefik.http.routers.openclaude-ext.tls=true"
      - "traefik.http.routers.openclaude-ext.tls.certresolver=cfdns"
      - "traefik.http.routers.openclaude-ext.tls.domains[0].main=shivelymedia.com"
      - "traefik.http.routers.openclaude-ext.tls.domains[0].sans=*.shivelymedia.com"
      - "traefik.http.routers.openclaude-ext.middlewares=globalHeaders@file,secureHeaders@file,authentik@docker"
      - "traefik.http.routers.openclaude-ext.service=openclaude-ext"
      - "traefik.http.services.openclaude-ext.loadbalancer.server.port=50051"
      - "traefik.http.services.openclaude-ext.loadbalancer.server.scheme=h2c"
      - "spiffe.io/spiffe-id=spiffe://home-ai-lab/inference/openclaude-grpc"

  # ═══════════════════════════════════════════════════════════════════════════════
  # IDENTITY — Authentik SSO is now managed by Saltbox (Phase 8B)
  # Container names: authentik, authentik-worker, authentik-postgres
  # Network: saltbox (Traefik bridges both saltbox and ai_lab_net)
  # ═══════════════════════════════════════════════════════════════════════════════

  # ═══════════════════════════════════════════════════════════════════════════════
  # DEVELOPMENT TOOLS (migrated from Lovelace, Phase 8)
  # ═══════════════════════════════════════════════════════════════════════════════

  # OPENHANDS - AI Development Agent (v1.5)
  openhands:
    image: docker.openhands.dev/openhands/openhands:1.5
    container_name: openhands_sandbox
    restart: unless-stopped
    environment:
      - AGENT_SERVER_IMAGE_REPOSITORY=ghcr.io/openhands/agent-server
      - AGENT_SERVER_IMAGE_TAG=1.12.0-python
      - LOG_ALL_EVENTS=true
    ports:
      - "3002:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - openhands_data:/.openhands
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.openhands.rule=PathPrefix(`/hands`)"
      - "traefik.http.routers.openhands.entrypoints=web"
      - "traefik.http.routers.openhands.middlewares=strip-hands,authentik@docker"
      - "traefik.http.middlewares.strip-hands.stripprefix.prefixes=/hands"
      - "traefik.http.services.openhands.loadbalancer.server.port=3000"

  # VS CODE SERVER (DevOps - Full Access, Protected by Authentik SSO)
  agent_ide_devops:
    image: lscr.io/linuxserver/code-server:latest
    container_name: agent_ide_devops
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - PASSWORD=${VSCODE_PASSWORD}
      - SUDO_PASSWORD=${VSCODE_SUDO_PASSWORD}
      - DEFAULT_WORKSPACE=/config/workspace
      - SWARM_API_KEY=sk-devops-identity
      - FREE_CODE_ENABLE=false
    volumes:
      - ../:/config/workspace
      - vscode_config_devops:/config
      - ../execution_plane/init_ide:/custom-cont-init.d:ro
    ports:
      - "8445:8443"  # 8443 used by Saltbox/Traefik on Turing
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ide-devops.rule=PathPrefix(`/devops`)"
      - "traefik.http.routers.ide-devops.entrypoints=web"
      - "traefik.http.routers.ide-devops.middlewares=strip-devops,authentik@docker"
      - "traefik.http.middlewares.strip-devops.stripprefix.prefixes=/devops"
      - "traefik.http.services.ide-devops.loadbalancer.server.port=8443"

  # VS CODE SERVER (Coding - Restricted Sandbox, Protected by Authentik SSO)
  agent_ide_coding:
    image: lscr.io/linuxserver/code-server:latest
    container_name: agent_ide_coding
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - PASSWORD=${VSCODE_PASSWORD}
      - SUDO_PASSWORD=${VSCODE_SUDO_PASSWORD}
      - DEFAULT_WORKSPACE=/config/workspace
      - SWARM_API_KEY=sk-coder-identity
      - FREE_CODE_ENABLE=true
    volumes:
      - ../user_projects:/config/workspace
      - vscode_config_coding:/config
      - ../execution_plane/init_ide:/custom-cont-init.d:ro
    ports:
      - "8444:8443"
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ide-coding.rule=PathPrefix(`/code`)"
      - "traefik.http.routers.ide-coding.entrypoints=web"
      - "traefik.http.routers.ide-coding.middlewares=strip-code,authentik@docker"
      - "traefik.http.middlewares.strip-code.stripprefix.prefixes=/code"
      - "traefik.http.services.ide-coding.loadbalancer.server.port=8443"

  # ═══════════════════════════════════════════════════════════════════════════════
  # OPS PORTAL — Admin Dashboard (migrated from Lovelace, Phase 8)
  # ═══════════════════════════════════════════════════════════════════════════════

  ops-portal:
    image: home-ai-lab/agent-runtime:latest
    container_name: ops_portal
    restart: unless-stopped
    user: "0"
    env_file:
      - ../network.env
    environment:
      - AGNO_DB_URL=${AGNO_DB_URL}
      - LANGFUSE_HOST=${LANGFUSE_HOST}
      - LANGFUSE_SECRET_KEY=${LANGFUSE_SECRET_KEY}
      - LANGFUSE_PUBLIC_KEY=${LANGFUSE_PUBLIC_KEY}
    volumes:
      - ../agents:/app/agents
      - ../docs:/app/docs
      - ../:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    working_dir: /workspace
    command: ["streamlit", "run", "agents/ops_dashboard.py", "--server.port=8502", "--server.address=0.0.0.0"]
    ports:
      - "8502:8502"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8502/_stcore/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 45s
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ops-portal.rule=PathPrefix(`/ops`)"
      - "traefik.http.routers.ops-portal.entrypoints=web"
      - "traefik.http.routers.ops-portal.middlewares=strip-ops,authentik@docker"
      - "traefik.http.middlewares.strip-ops.stripprefix.prefixes=/ops"
      - "traefik.http.services.ops-portal.loadbalancer.server.port=8502"

  ntfy:
    image: binwiederhier/ntfy:latest
    container_name: ntfy-Turing
    hostname: ntfy
    restart: unless-stopped
    init: true
    command: ["serve"]
    environment:
      - TZ=Etc/UTC
      - NTFY_BASE_URL=${NTFY_BASE_URL:-https://notify.shivelymedia.com}
      - NTFY_LISTEN_HTTP=:80
      - NTFY_CACHE_FILE=/var/lib/ntfy/cache.db
      - NTFY_AUTH_FILE=/var/lib/ntfy/auth.db
      - NTFY_AUTH_DEFAULT_ACCESS=deny-all
      - NTFY_AUTH_USERS=${NTFY_ADMIN_USER:?set NTFY_ADMIN_USER}:${NTFY_ADMIN_PASSWORD_HASH:?set NTFY_ADMIN_PASSWORD_HASH (bcrypt) — see turing_gateway/.env}:admin
      - NTFY_AUTH_TOKENS=${NTFY_ADMIN_USER}:${NTFY_ALERT_TOKEN:?set NTFY_ALERT_TOKEN — see turing_gateway/.env}:alertmanager
      - NTFY_BEHIND_PROXY=true
      - NTFY_ENABLE_LOGIN=true
      - NTFY_ENABLE_METRICS=true
      - NTFY_UPSTREAM_BASE_URL=https://ntfy.sh
    volumes:
      - ntfy_data:/var/lib/ntfy
    ports:
      - "8086:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --tries=1 http://localhost:80/v1/health -O - | grep -Eo '\"healthy\"\\s*:\\s*true' || exit 1"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - ai_lab_net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ntfy.rule=Host(`notify.shivelymedia.com`)"
      - "traefik.http.routers.ntfy.entrypoints=websecure"
      - "traefik.http.routers.ntfy.priority=20"
      - "traefik.http.routers.ntfy.tls=true"
      - "traefik.http.routers.ntfy.tls.certresolver=cfdns"
      - "traefik.http.routers.ntfy.tls.domains[0].main=shivelymedia.com"
      - "traefik.http.routers.ntfy.tls.domains[0].sans=*.shivelymedia.com"
      - "traefik.http.routers.ntfy.tls.options=securetls@file"
      - "traefik.http.routers.ntfy.middlewares=globalHeaders@file,secureHeaders@file,robotHeaders@file"
      - "traefik.http.services.ntfy.loadbalancer.server.port=80"
      - "traefik.http.routers.ntfy-http.rule=Host(`notify.shivelymedia.com`)"
      - "traefik.http.routers.ntfy-http.entrypoints=web"
      - "traefik.http.routers.ntfy-http.priority=20"
      - "traefik.http.routers.ntfy-http.middlewares=globalHeaders@file,redirect-to-https@docker,robotHeaders@file"
      - "traefik.http.routers.ntfy-http.service=ntfy"

  # ── Documentation Site ──────────────────────────────────────────
  docs-site:
    build:
      context: ../docs-site
      dockerfile: Dockerfile
    container_name: docs-site
    hostname: docs-site
    restart: unless-stopped
    networks:
      - ai_lab_net
      - saltbox
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=saltbox"
      # LAN route
      - "traefik.http.routers.docs.rule=PathPrefix(`/docs-site`)"
      - "traefik.http.routers.docs.entrypoints=web"
      - "traefik.http.routers.docs.middlewares=docs-strip"
      - "traefik.http.routers.docs.service=docs"
      - "traefik.http.services.docs.loadbalancer.server.port=80"
      - "traefik.http.middlewares.docs-strip.stripprefix.prefixes=/docs-site"
      # External HTTPS route (memex.shivelymedia.com/docs-site)
      - "traefik.http.routers.docs-memex.rule=Host(`memex.shivelymedia.com`) && PathPrefix(`/docs-site`)"
      - "traefik.http.routers.docs-memex.entrypoints=websecure"
      - "traefik.http.routers.docs-memex.priority=30"
      - "traefik.http.routers.docs-memex.tls=true"
      - "traefik.http.routers.docs-memex.middlewares=docs-strip"
      - "traefik.http.routers.docs-memex.service=docs"

networks:
  ai_lab_net:
    driver: bridge
    name: ai_lab_net
  saltbox:
    external: true
    name: saltbox

volumes:
  spire_agent_data:
    external: true
    name: turing_gateway_spire_agent_data
  spire_socket:
    external: true
    name: turing_gateway_spire_socket
  prometheus_data:
    external: true
    name: r730_gateway_prometheus_data
  alertmanager_data:
    external: true
    name: r730_gateway_alertmanager_data
  loki_data:
    external: true
    name: r730_gateway_loki_data
  grafana_data:
    external: true
    name: r730_gateway_grafana_data
  redis_data:
    external: true
    name: r730_gateway_redis_data
  ntfy_data:
    external: true
    name: r730_gateway_ntfy_data
  ollama_models:
    external: true
    name: r730_gateway_ollama_models
  open-webui_data:
    external: true
    name: r730_gateway_open-webui_data
  # Authentik volumes removed — now managed by Saltbox (Phase 8B)
  openhands_data:
    external: true
    name: r730_gateway_openhands_data
  # Code Servers (migrated from Lovelace)
  vscode_config_devops:
    external: true
    name: r730_gateway_vscode_config_devops
  vscode_config_coding:
    external: true
    name: r730_gateway_vscode_config_coding



